const user = (state = {}, action) => {
  switch (action.type) {
    case 'SIGN_UP': {
      return state;
    }
    case 'LOGIN': {
      return state;
    }
    default: {
      return state;
    }
  }
};

export default user;
